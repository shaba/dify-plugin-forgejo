[project]
name = "dify-plugin-forgejo"
version = "0.0.1"
description = "Browse repositories, issues and pull requests on any Forgejo or Gitea server"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "dify_plugin>=0.9.0",
    "requests>=2.32",]

[dependency-groups]
dev = []
